Archived legacy and unused code
================================

- legacy_root_pkg/: previous root-level limo_control package copy.
  - package.xml.disabled prevents catkin from discovering this archive as a package.
- unused/: misc backups and old launch files.

Nothing under this folder is built or installed. Keep for reference.
